version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/voting-app

hooks:
  BeforeInstall:
    - location: scripts/cleanup.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 900
      runas: root
